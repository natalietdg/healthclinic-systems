import React from 'react';
import { Route, Switch } from 'react-router';

const publicRoutes = [
    {}
]

const privateRoutes = [
    {}
]

export const routes = {
    public: publicRoutes,
    private: privateRoutes
}